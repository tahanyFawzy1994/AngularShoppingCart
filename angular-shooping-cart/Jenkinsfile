pipeline {

    agent any
    tools {nodejs "node"}
    stages {
      
        stage('Build') {
            steps {
                echo 'Building...'
                sh 'npm install'
            }
		}
          stage('Run') {
            steps {
                echo 'Running...'
                bat 'npm start'
            }
        
        }
    }
}
